# Release Notes

## v1.0.0 — 2025-12-10
- Introduced versioned layout (`contexts/`, `schemas/`, `payload-schemas/`, `examples/`, `metadata/`, `docs/`) with `v1.0/` folders for immutable artifacts.
- Published Ayra Business Card JSON-LD context (`AyraBusinessCardV1R0.jsonld`) and JSON Schema with canonical raw GitHub URLs.
- Added unified payload schema plus legacy payload schema (deprecated) under `payload-schemas/v1.0/`.
- Supplied credential and credentialSubject example payloads under `examples/v1.0/`.
